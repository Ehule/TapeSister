# TapeSister notices

The Phase 1C UI code and 5x7 bitmap font are original TapeSister work. No FT2
bitmap, font, icon, or other CC BY-NC-SA graphical asset is copied into this
directory. The 632x400 indexed-framebuffer approach and palette-file field
names are interoperability conventions informed by the BSD-licensed FT2 Clone
source in the parent incubation repository.

SDL2 is used only when `TAPESISTER_BUILD_SDL=ON` and is not vendored here.

